# intent-gate
Minimal refusal boundary that blocks destructive filesystem actions unless an explicit Intent Record exists. Demonstrates intent-gated execution for agentic systems. V1: local CLI, deterministic allow/deny, auditability.
